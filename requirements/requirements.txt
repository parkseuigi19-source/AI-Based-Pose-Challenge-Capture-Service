# 영상 및 이미지 처리
mediapipe==0.10.21              # 이미지 내 손 or 포즈의 스켈레톤 입력 및 추출
opencv-python==4.8.0.76         # 영상 처리(영상 불러오고 프레임 단위로 처리 가능)

# 데이터 처리
numpy==1.26.4                   # 고성능 수치 계산과 배열(행렬) 처리(영상 데이터, 딥러닝 데이터, 라벨 데이터 등 숫자 배열을 효율적으로 다루기 위해 사용)
pandas==2.1.1                   # 데이터 분석, 라벨/결과 처리(영상 라벨(json, csv, txt 등)을 불러오고, 데이터프레임으로 정리/가공할 때 필요)

# 웹 시각화
fastapi==0.115.0                # 파이썬에서 HTML, CSS, JS를 활용한 웹 앱 구현 가능
uvicorn[standard]==0.30.6       # FastAPI 서버 실행용
jinja2==3.1.4                   # HTML 템플릿 렌더링
python-multipart==0.0.6         # Form 데이터를 처리할 때 필요
tqdm==4.66.5                    # 반복문(루프)의 진행 상황을 한눈에 볼 수 있게 해주는 진행률 확인 가능

# YOLO
ultralytics>=8.0.196            # YOLO

# os, shutil, json 설치 필요 X - 파일/폴더 관리 및 데이터 불러오기
